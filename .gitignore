# OS
.DS_Store
Thumbs.db
*.swp

# JetBrains
.idea/

# Les sous-projets (BackendLaravel, FrontFlutter, FrontWeb) ont leurs propres
# .gitignore qui couvrent vendor/, node_modules/, build/, .env, etc.
# La config VS Code de FrontFlutter (.vscode/settings.json, launch.json) est
# volontairement conservée car elle porte le SDK Flutter géré par FVM.
