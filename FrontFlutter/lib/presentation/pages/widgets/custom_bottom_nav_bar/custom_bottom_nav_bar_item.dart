import 'package:flutter/widgets.dart';

/// Élément de la barre de navigation inférieure (icône + libellé).
class CustomBottomAppBarItem {
  final IconData icon;
  final String text;
  const CustomBottomAppBarItem({required this.icon, required this.text});
}
